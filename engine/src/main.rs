//! CLI entry point for the engine. The scenario runner lands in Epic 06.

fn main() {
    println!(
        "termocline engine (format version {}) — no scenario runner yet, see Epic 06",
        engine::FORMAT_VERSION
    );
}
