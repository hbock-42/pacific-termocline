//! CLI entry point for the visualizer. The UI lands in Epic 08.

fn main() {
    println!(
        "termocline visualizer (format version {}) — no rendering yet, see Epic 08",
        visualizer::FORMAT_VERSION
    );
}
