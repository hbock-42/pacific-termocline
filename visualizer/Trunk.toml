# `trunk serve` from this directory builds the browser visualizer and serves
# it at http://localhost:8080. There is no run to hand it: the page computes
# one itself from a scenario in `scenarios/`, because the run format is not
# served to the browser at all (ADR-0012).
[build]
target = "index.html"

[serve]
port = 8080
